{"name":"Publish site","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"publish":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"name":"Fetch build","run":"curl -sSL -o site.zip https://jkqkasucyievrlkjoehw.supabase.co/storage/v1/object/public/relay/site.zip && rm -rf docs && mkdir -p docs && unzip -q site.zip -d docs && rm site.zip && touch docs/.nojekyll && ls docs"},{"name":"Commit","run":"git config user.name github-actions && git config user.email actions@github.com && git add -A docs && (git commit -m 'Publish Restaurant Day web build' || echo no-changes) && git push"}]}}}
